% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oklch.R
\name{max_chroma}
\alias{max_chroma}
\title{The most chroma sRGB can show}
\usage{
max_chroma(L, H)
}
\arguments{
\item{L}{Lightness, \code{0}-\code{1}.}

\item{H}{Hue in degrees.}
}
\value{
A length-1 numeric. Hard-capped at \code{0.4}: a hue whose true ceiling exceeds it returns ~0.4.
}
\description{
\code{oklch_hex()} clamps to this silently; \code{max_chroma()} is what lets a caller SEE the clamp instead.
A palette must never record a chroma it does not actually have.
}
\examples{
max_chroma(0.95, 100)   # 0.107 -- which is why the ladder's top rung asks for 0.10
max_chroma(0.96, 100)
}
