% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-theme.R
\name{build_theme}
\alias{build_theme}
\title{Regenerate every theme artifact}
\usage{
build_theme(path = ".", check = FALSE, quiet = FALSE)
}
\arguments{
\item{path}{The package root.}

\item{check}{Do not write: rebuild every output in memory and compare it with the file on disk.
\code{TRUE} when they all match. This is what the test suite asserts, and it is only meaningful
because nothing generated carries a date.}

\item{quiet}{Suppress the per-file message and the contrast report.}
}
\value{
Invisibly, \code{TRUE} if everything is in step (always \code{TRUE} after a write).
}
\description{
Validates the grids, then writes the pkgdown layer, the flat highlight sheets, the Quarto
\code{.scss} / \code{.theme} pairs, the two brand files, the annotations asset and the editor block. Run it
by hand whenever a colour changes; never at render time.
}
\examples{
\dontrun{
build_theme("~/github/txtheme")
build_theme("~/github/txtheme", check = TRUE)
}
}
