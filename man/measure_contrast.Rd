% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{measure_contrast}
\alias{measure_contrast}
\title{Measure the contrast of an element as the page paints it}
\usage{
measure_contrast(
  page,
  selector,
  mode = c("light", "dark"),
  actions = NULL,
  width = 1280,
  height = 900,
  delay = 0.5
)
}
\arguments{
\item{page}{Path to a rendered \code{.html}, or a \verb{file://} / \verb{http://} URL.}

\item{selector}{A CSS selector. Several may be given; each is measured on its first match.}

\item{mode}{\code{"light"}, \code{"dark"}, or both (the default) -- one file each.}

\item{actions}{Character vector, played in order after \code{load} and before the shot, and replayed
identically in each mode. Four verbs: \code{"click:<css>"}, \code{"fill:<css>=<value>"},
\code{"wait:<ms>"}, \code{"js:<expression>"}. A \code{click} or a \code{fill} that matches nothing is an error.}

\item{width, height}{The viewport, in CSS pixels. Layout depends on it; \code{width = 390} is a phone.
The element may be taller than the window: it is captured whole either way.}

\item{delay}{Seconds to wait after \code{load}, and again after the actions, before measuring.}
}
\value{
A data frame, one row per selector and mode: \code{mode}, \code{selector}, \code{n} (the number of
matches), \code{text} and \code{background} (\code{"#RRGGBB"}), \code{wcag} and \code{apca}. Printed as it is measured.
}
\description{
Opens a rendered page in headless Chromium, once per mode, and reads the text colour and the
ground of the FIRST element \code{selector} matches, as painted: translucent backgrounds are
composited down the ancestor chain, and any CSS colour syntax is resolved to sRGB. Then reports
\code{\link[=contrast]{contrast()}} (WCAG 2.x) and \code{\link[=apca]{apca()}} on the two.
}
\details{
Only \code{background-color} is seen: a ground painted by an image, a gradient, an inset \code{box-shadow}
or \code{opacity} is not.
}
\examples{
\dontrun{
measure_contrast("page.html", c(".webex-inset[data-couleur='terrain'] > .webex-title",
                                ".webex-box > .exercise > p > .theorem-title"))
}
}
