% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capture.R
\name{screenshot}
\alias{screenshot}
\title{A screenshot of one element, in both modes}
\usage{
screenshot(
  page,
  selector,
  mode = c("light", "dark"),
  actions = NULL,
  width = 1280,
  height = 900,
  scale = 1,
  expand = 8,
  dir = NULL,
  name = NULL,
  delay = 0.5
)
}
\arguments{
\item{page}{Path to a rendered \code{.html}, or a \verb{file://} / \verb{http://} URL.}

\item{selector}{A CSS selector. Several matches are allowed: the shot is the bounding box of the
ones that are painted, and the count is announced. No match, or nothing painted, is an error.}

\item{mode}{\code{"light"}, \code{"dark"}, or both (the default) -- one file each.}

\item{actions}{Character vector, played in order after \code{load} and before the shot, and replayed
identically in each mode. Four verbs: \code{"click:<css>"}, \code{"fill:<css>=<value>"},
\code{"wait:<ms>"}, \code{"js:<expression>"}. A \code{click} or a \code{fill} that matches nothing is an error.}

\item{width, height}{The viewport, in CSS pixels. Layout depends on it; \code{width = 390} is a phone.
The element may be taller than the window: it is captured whole either way.}

\item{scale}{Device pixel ratio of the output. \code{1} by default, which is legible for prose and
layout and costs a quarter of what \code{2} does. Raise it to \code{2} to judge a colour or a hairline --
a border, a focus ring; above that nothing is gained, since a content-column element then
passes the 1568 px at which an image is shrunk again before being read.}

\item{expand}{Padding around the element, in pixels, so a border or a shadow is not clipped.}

\item{dir}{Where the files go. Defaults to this package's user cache directory.}

\item{name}{Base name for the files, without extension or mode. Derived from the page and the
selector when left \code{NULL}, so a second call to the same element overwrites the first.}

\item{delay}{Seconds to wait after \code{load}, and again after the actions, before measuring.}
}
\value{
The written paths, invisibly.
}
\description{
Loads a rendered page in a headless Chromium and writes a PNG of the element \code{selector} names --
the browser's "screenshot node", not a screenshot of the window. One file per mode, each one
announced with its pixel size and with what it costs a model to read.
}
\details{
The mode is imposed through \code{prefers-color-scheme} and then verified against whatever hook the
page uses to record it (\code{body.quarto-dark} for a Quarto page, \code{data-bs-theme} for a Bootstrap or
pkgdown one). A page that ends up in the other mode stops the call rather than returning a
plausible-looking picture of the wrong thing.

The page is driven only after its \code{load} event, because that is when a Quarto page finishes
building itself: the light/dark switch, and anything a filter's javascript adds, do not exist
before it.

What is captured is the element's border box, padded by \code{expand} -- an exercise's frame is part
of what one wants to see -- cut down by every ancestor that clips it. An element a closed
solution or a collapsed panel hides is therefore refused rather than captured: it is laid out
where it says, and painted nowhere. Open it first with an \code{actions} click.
}
\examples{
\dontrun{
screenshot("dev/preview_theme.html", "#un-tableau-tabxplor")
screenshot("_site/cours/M1S1/M1S1_02.html", "table.tabxplor-tab", mode = "dark", scale = 2)
screenshot("dev/exercices_types.html", "div.webex-box:has(> div#exr-m1-lecture)",
           actions = c("fill:input.webex-solveme=42", "click:button.webex-check-button"))
}
}
